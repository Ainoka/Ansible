###To check user UID 
#ansible -i inventory/devinventory dev-servers -m shell -a  "cat /etc/passwd"

### To see the permissions files under /tmp: ls -l
#ansible -i inventory/devinventory dev-servers -m command -a "ls -l /tmp"
### To
#ansible -i inventory/devinventory dev-servers -m shell -a "cat /etc/passwd"